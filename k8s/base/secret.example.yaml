# ---------------------------------------------------------------------------
# IMPORTANT: this file is a TEMPLATE only. Do NOT apply it as-is.
# The real secret is generated by the Ansible playbook 06-deploy-app.yml using
# values from ansible-vault. Sample base64 values below are placeholders that
# decode to the literal string 'changeme' so committing this file is safe.
# ---------------------------------------------------------------------------
apiVersion: v1
kind: Secret
metadata:
  name: taskmanager-secrets
  namespace: deployforge
type: Opaque
data:
  DB_USERNAME: dGFza3VzZXI=          # base64('taskuser')
  DB_PASSWORD: Y2hhbmdlbWU=          # base64('changeme') — REPLACE in production
  POSTGRES_USER: dGFza3VzZXI=
  POSTGRES_PASSWORD: Y2hhbmdlbWU=
  POSTGRES_DB: dGFza2Ri              # base64('taskdb')
